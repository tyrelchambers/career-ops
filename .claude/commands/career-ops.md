---
description: AI job search command center -- show menu or evaluate job description
---

Invoke the `career-ops` skill via the Skill tool.

Arguments: $ARGUMENTS

If arguments contain a job description or URL (keywords like "responsibilities", "requirements", "qualifications", "about the role", "http", "https"), run auto-pipeline mode. Otherwise show the discovery menu.
